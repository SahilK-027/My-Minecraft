/**
 * id: represents whether the block at (x,y,z) is grass / water / stone etc.
 * instanceId: represents reference to actual mesh reference at (x,y,z) 
 * @type {{
 * id: number, 
 * instanceId: number
 * }}
 */

export const data = [];